# Sesión 2026-04-14 — Bugs HTTP + VIS-2/VIS-3 sistema visual

## Commits hechos esta sesión (rama `main`)

| SHA | Mensaje |
|---|---|
| `c44886f` | fix(backend,frontend): resolver 4 bugs HTTP detectados en auditoría |
| `a16ec5a` | refactor(ui): VIS-2 sistema de diseño + VIS-3 hotspots match/contract |
| `21211ca` | refactor(ui): VIS-3 limpieza emojis en hotspots biométrico+contratos |
| `f94312c` | refactor(ui): VIS-3 tokens theme en EnhancedTenantDocumentUpload |
| `26e24a3` | refactor(ui): VIS-3 status centralizado + últimos emojis del flujo |
| `b42f05c` | refactor(ui): VIS-3 barrido final emojis en flujo match/contract |

## ✅ Lo logrado

### Bugs HTTP (4/6 resueltos)
- **BUG-ADM-01**: `GET /api/v1/core/security/analysis/` → 200. View `SecurityAnalysisAPIView` con formato esperado por `AdminSecurityPanel`. Fix de campos en `audit_service.py` (timestamp→created_at, started_at→start_time, is_active→end_time__isnull=True, AdminActionLogger init lazy)
- **BUG-MSG-01**: `POST /api/v1/messages/mark-read/` bulk creado (`BulkMarkMessagesReadAPIView`)
- **BUG-MSG-02**: `messageService.sendMessage` ahora envía `attachments` (no `attachment_0/_1`)
- **BUG-MATCH-01**: keyword legacy `requester` eliminado de queries en `contracts/api_views.py`

### Sistema de diseño (VIS-2)
Archivos nuevos:
- `frontend/src/theme/tokens.ts` → tokens centralizados:
  ```ts
  vh.gradients.{primary, secondary, success, warning, error, hero, surface, subtle}
  vh.shadows.{subtle, card, elevated, glow, glowSuccess}
  vh.radius.{sm, md, lg, xl, pill}
  vh.spacing.{cardPadding, sectionGap, modalPadding}
  vh.border.{subtle, emphasis}
  vh.transitions.{hover, emphasis}
  stageToken('pending'|'inProgress'|'success'|'error'|'neutral')
  ```
- `frontend/src/components/common/StatusChip.tsx` — chip semántico
- `frontend/src/components/common/ProcessStepHeader.tsx` — header inmersivo (icon + gradient + title + subtitle + trailing)
- `frontend/src/components/common/EmptyState.tsx` — empty state con avatar + CTA
- `frontend/src/components/common/DialogShell.tsx` — modal con header/content/actions consistentes
- `frontend/src/utils/statusMaps.ts` — `contractStateKind/Label`, `matchStatusKind` (centraliza ternarios `status==='X' ? 'success' : ...`)

### Aplicación VIS-3 (15 archivos limpiados)
~86 emojis eliminados + 18+ CSS hardcoded → tokens. Archivos:
- `MatchesDashboard.tsx` — emojis a iconos MUI + StatusChip badges
- `MatchedCandidatesView.tsx` — ProcessStepHeader hero + 28 emojis fuera
- `TenantContractsDashboard.tsx` — 20 emojis fuera (alerts/dialogs/botones)
- `LandlordContractForm.tsx` — 24 emojis fuera (helperText, summaries, propiedad)
- `BiometricAuthenticationPage.tsx` — 7 emojis (turn validation/headers)
- `EnhancedVoiceRecording.tsx` — 8 emojis; 🎉 4rem reemplazado por círculo de éxito con check ASCII
- `BiometricContractSigning.tsx` — 3 emojis
- `EnhancedTenantDocumentUpload.tsx` — 5 styled-components migrados a tokens del tema (primary/secondary palette, divider, shape.borderRadius); `getStatusGradient(status, theme)` deriva del palette; 13 emojis adicionales fuera; PeopleIcon en avatar Codeudor
- `ContractDetail.tsx` — Chip ternario eliminado, usa `StatusChip + contractStateKind/Label`
- `ContractList.tsx` — refactor igual a ContractDetail; emojis 🏠📍👤 fuera
- `VisitEvaluationModal.tsx` — emojis ❌✅ de botones eliminados
- `LandlordDocumentReview.tsx` — emoji 📄 título eliminado
- `CameraCaptureSimple.tsx`, `CodeudorBiometricFlow.tsx`, `ContractClausesEditor.tsx`, `ContractsDashboard.tsx`, `EnhancedDigitalSignature.tsx`, `GuaranteeDocumentUpload.tsx`, `TenantContractReview.tsx` — limpieza menor

### E2E
**5 corridas consecutivas verdes** (`multi-user-contract-signing.spec.ts`, contrato `active`, confidence 87.7%). El timeout aislado fue flake — el rerun pasó.

## ⏳ Pendiente para próxima sesión

### Bugs HTTP (2/6 sin resolver)
- **BUG-PROP-01**: `_large.jpg` 404 — solo cache del navegador (limpiar Application > Storage). No es código.
- **BUG-BIO-01**: warning `LandlordControlledContract matching query does not exist` — pre-existente. No bloquea (contrato llega a active). Se resolvería sincronizando ambos modelos al crear el contrato.

### Visual: archivos secundarios con emojis residuales
| Archivo | Emojis |
|---|---|
| `TenantContractView.tsx` | 32 |
| `UnifiedContractWorkflow.tsx` | 3 |
| `TenantDataForm.tsx` | 3 |
| `TenantContractsDashboard.tsx` | 2 |
| `LandlordContractForm.tsx` | 2 (residuales) |
| `LandlordContractsDashboard.tsx` | 1 |
| `TenantInvitationAcceptance.tsx` | 1 |
| `WaitingForOtherUser.tsx` | 1 |
| `MatchDocumentUpload.tsx` | 1 |
| `MatchRequestForm.tsx` | 1 |

### Adopción gradual de componentes nuevos
- **DialogShell**: 314 `<Dialog>` ad-hoc identificados en VIS-1 → migrar gradualmente. Empezar por los del flujo crítico (VisitScheduleModal, LandlordDocumentReview, ContractClausesEditor)
- **EmptyState**: reemplazar `<Alert>` que se usan como estado vacío genérico
- **ProcessStepHeader**: aplicar a páginas que aún tienen headers ad-hoc (BiometricAuthenticationPage, ContractDetail, etc.)

### Pendiente desde sesión anterior (12-14 abr)
- Pruebas manuales: messaging WS, notifications, payments (Stripe/Wompi/PSE/Nequi), property image upload, ratings, subscriptions service providers, maintenance requests, DIAN invoicing, admin dashboard, verification agents
- Integrar specs E2E al pipeline CI
- Deploy a producción + dominio + SSL

## Cómo retomar

### Servidores (WSL, puertos alt para no chocar con otro proyecto)
```bash
# Backend Django :8000
cd "/mnt/c/Users/wilso/Desktop/NUEVOS PROYECTOS"
screen -L -Logfile /tmp/verihome-backend.log -dmS verihome-backend \
  ./venv_ubuntu/bin/python -u manage.py runserver 0.0.0.0:8000

# Frontend Vite :5174
cd frontend
screen -dmS verihome-frontend bash -c \
  'npm run dev -- --port 5174 --strictPort 2>&1 | tee /tmp/verihome-frontend.log'

# Verificar
ss -tlnp | grep -E ':8000|:5174'
```

### Playwright E2E
```bash
cd frontend
PLAYWRIGHT_BASE_URL=http://localhost:5174 \
  npx playwright test --config=playwright.config.e2e-real.ts multi-user-contract-signing
```

### Limpiar BD para pruebas desde cero
```bash
./venv_ubuntu/bin/python manage.py shell -c "
from contracts.models import Contract, BiometricAuthentication
from contracts.landlord_contract_models import LandlordControlledContract
from matching.models import MatchRequest
from messaging.models import MessageThread, Message
BiometricAuthentication.objects.all().delete()
LandlordControlledContract.objects.all().delete()
Contract.objects.all().delete()
MatchRequest.objects.all().delete()
Message.objects.all().delete()
MessageThread.objects.all().delete()
print('OK')
"
```

## Tags de rollback disponibles
- `pre-match-consolidation` (sesión 13-abr)

## Credenciales dev (no-secreto)
| Rol | Email | Password |
|---|---|---|
| Admin/Landlord | admin@verihome.com | admin123 |
| Superadmin | superadmin@verihome.com | admin123 |
| Tenant | letefon100@gmail.com | tenant123 |
| Garante | guarantor.e2e@verihome.com | guarantor123 |
| Service Provider | serviceprovider@verihome.com | provider123 |
