/**
 * Re-export wrapper for backward compatibility.
 * The real implementation lives in useRequests.ts → useMaintenanceRequests().
 */
export { useMaintenanceRequests as useMaintenance } from './useRequests';
export { useMaintenanceRequests } from './useRequests';
